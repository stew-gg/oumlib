package dev.oum.oumlib.inventory;

public enum GuiAction {
    NOTHING,
    REFRESH,
    CLOSE,
    PAGE_FIRST,
    PAGE_BACKWARDS,
    PAGE_FORWARDS,
    PAGE_LAST,
    TRANSITION_FORWARDS,
    TRANSITION_BACKWARDS
}
